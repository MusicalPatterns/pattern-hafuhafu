// tslint:disable no-magic-numbers

import { as, Cardinal, negative } from '@musical-patterns/utilities'
import { Layer } from '../../../nominals'

const DROSTE_SIEVE_FRACTAL_LAYER_INITIAL_INDEX_COMPUTATION_PARTIAL_POWER_SUM_SHIFT: Cardinal<Cardinal<Layer[]>> =
    as.Cardinal<Cardinal<Layer[]>>(negative(3))

export {
    DROSTE_SIEVE_FRACTAL_LAYER_INITIAL_INDEX_COMPUTATION_PARTIAL_POWER_SUM_SHIFT,
}
