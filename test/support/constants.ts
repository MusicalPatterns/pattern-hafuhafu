import { as, Ordinal } from '@musical-patterns/utilities'

const INDEX_OF_DURATION_IN_CONTOUR: Ordinal = as.Ordinal(1)
const INDEX_OF_GAIN_IN_CONTOUR: Ordinal = as.Ordinal(2)
const INDEX_OF_ENVELOPE_IN_CONTOUR: Ordinal = as.Ordinal(3)
const INDEX_OF_PITCH_SCALAR_IN_CONTOUR: Ordinal = as.Ordinal(4)

export {
    INDEX_OF_DURATION_IN_CONTOUR,
    INDEX_OF_GAIN_IN_CONTOUR,
    INDEX_OF_ENVELOPE_IN_CONTOUR,
    INDEX_OF_PITCH_SCALAR_IN_CONTOUR,
}
