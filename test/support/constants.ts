import { Ordinal, to } from '@musical-patterns/utilities'

const INDEX_OF_DURATION_IN_CONTOUR: Ordinal = to.Ordinal(1)
const INDEX_OF_GAIN_IN_CONTOUR: Ordinal = to.Ordinal(2)
const INDEX_OF_SUSTAIN_IN_CONTOUR: Ordinal = to.Ordinal(3)
const INDEX_OF_PITCH_SCALAR_IN_CONTOUR: Ordinal = to.Ordinal(4)

export {
    INDEX_OF_DURATION_IN_CONTOUR,
    INDEX_OF_GAIN_IN_CONTOUR,
    INDEX_OF_SUSTAIN_IN_CONTOUR,
    INDEX_OF_PITCH_SCALAR_IN_CONTOUR,
}
